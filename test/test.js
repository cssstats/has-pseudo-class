var assert = require('assert');
var hasPseudoClass = require('..');

describe('has-pseudo-class', function() {

  it('should do something awesome', function() {
    assert.equal(hasPseudoClass(), true);
  });
});
